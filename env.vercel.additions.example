# Coverlist — Vercel Env Additions (only keys missing on Vercel)
# Use this to import ONLY the variables not already present in your Vercel project.
# Paste into Vercel → Project Settings → Environment Variables → Import .env file.

# Public flags (frontend)
NEXT_PUBLIC_AUTH_USE_FIREBASE=true
# Sign-in mode: popup | redirect | auto (redirect is passkey-friendly)
NEXT_PUBLIC_GOOGLE_SIGNIN_MODE=redirect
# Optional: show extra auth debug UI on /login
NEXT_PUBLIC_AUTH_DEBUG=false

# Server (admin SDK) — required for session cookies, avatar backfill, etc.
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# IMPORTANT: Quote the private key and keep literal \n in Vercel
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...snip...\n-----END PRIVATE KEY-----\n"

# Note: This file intentionally omits keys you already have configured on Vercel
# (e.g., JWT_SECRET, NEXT_PUBLIC_FIREBASE_* client keys, Spotify/GitHub vars, etc.)
# Import will not overwrite anything unless a key in this file matches an existing key.
