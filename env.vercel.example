# Coverlist — Vercel Environment Variables (example)
# Copy these into Vercel Project Settings → Environment Variables
# or use Vercel's "Import .env file" with this file's contents.

# -----------------------------
# Public (exposed to browser)
# -----------------------------
# Enable Firebase client auth (required for Google sign-in UI)
NEXT_PUBLIC_AUTH_USE_FIREBASE=true

# Google sign-in mode: popup | redirect | auto
# - redirect is recommended for passkeys/WebAuthn (e.g., Windows Hello, iOS/Safari)
NEXT_PUBLIC_GOOGLE_SIGNIN_MODE=redirect


# Optional: show extra auth debug info on /login
NEXT_PUBLIC_AUTH_DEBUG=false

# -----------------------------
# Server (NOT exposed to browser)
# -----------------------------
# Firebase Admin SDK credentials
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# If pasting a raw multiline private key, surround with quotes and let the app convert \n
# Example shown as single line with literal \n escapes (works on Vercel):
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...snip...\n-----END PRIVATE KEY-----\n"

# App secrets
JWT_SECRET=replace-with-strong-random-secret

# -----------------------------
# Optional settings
# -----------------------------
# DATA_BACKEND=firestore                 # if you want DB mirroring to Firestore
# FIREBASE_DB_DOC=coverlist/db           # Firestore doc path for mirroring
